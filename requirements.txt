# Core dependencies
torch>=2.0.0
numpy>=1.24.0
Pillow>=10.0.0
PyYAML>=6.0

# Vector database
qdrant-client>=1.7.0

# Embedding models
FlagEmbedding>=1.2.10  # For BGE-M3 model (includes sentence-transformers)

# Document parsing
docling>=1.0.0  # For parsing documents (PDF, DOCX, PPTX, etc.)
PyPDF2>=3.0.0  # PDF parsing (Fallback)
python-docx>=0.8.11  # Word document parsing (Fallback)
pandas>=2.0.0  # Excel parsing (Fallback)
openpyxl>=3.1.0  # Excel support

# Database and system
psutil>=5.9.0
aiohttp>=3.9.0         # For asynchronous web requests
beautifulsoup4>=4.12.0 # For parsing HTML
lxml>=5.0.0            # High-performance parser for BeautifulSoup

# Web framework
fastapi>=0.104.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
uvicorn>=0.24.0

# Authentication and OAuth
authlib>=1.2.0  # For Google OAuth 2.0
python-jose[cryptography]>=3.3.0  # For JWT token handling
httpx>=0.25.0  # For async HTTP requests

# LangChain and AI
langchain-community>=0.0.20
langchain-core>=0.1.0
langgraph>=0.0.40
google-generativeai>=0.3.0

# Data visualization
plotly>=5.17.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.66.0
pywin32>=305; sys_platform == 'win32'  # For Windows system information collection
keyring>=24.0.0  # For secure token storage
apscheduler>=3.10.0 # For background task scheduling